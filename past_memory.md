# Past Memory Log

- **2026-04-23**:
  - Initialized project AI documentation.
  - Analyzed `KeyVisualizer_v2.html`. App is a single-page HTML/JS/CSS tool for parsing QMK JSON layouts and visualizing wiring paths.
  - Defined standard MCU profiles (RP2040, Pro Micro, nice!nano) internally in JS.
  - Key functionality includes a "mirror" mode for bottom view (soldering).
  - Implemented documentation scaffold: `README.md`, `scaffold.md`, `GEMINI.md`, and this file (`past_memory.md`) to comply with user global rules.
  - Fixed `keyboard.json` right-half matrix indices to be properly mirrored for a symmetric handwired layout.
  - Corrected `pid` to non-zero `0x6060` in `keyboard.json`.
  - Replaced invalid `RM_` prefixed RGB keycodes with standard `RGB_` QMK keycodes in `keymap.c` Layer 3.
  - Reverted `keyboard.json` matrix and PID changes as requested because physical wiring was already locked in.
  - Added SVG wiring diagram feature to `KeyVisualizer_v2.html` to visually connect matrix rows and columns.
  - Corrected previous AI's mistake: Reverted Right-Half inner thumb key from `[9,0]` back to `[9,5]` in `keyboard.json`. Symmetric left-to-right wiring means the inner thumb is connected to GP5 (Col 5) on both halves.
  - Added missing Layer 3 (Adjust) back to `keymap.c` to prevent firmware crashes when `MO(3)` is pressed.
  - Upgraded `KeyVisualizer_v2.html` "Show Wiring" SVG logic to use standard colored ribbon cable colors instead of static red/blue lines.
  - Implemented client-side `keymap.c` parsing in `KeyVisualizer_v2.html` using a parenthesis-tracking algorithm to handle nested QMK macros correctly.
  - Added a keycode translation dictionary to the visualizer to render raw `KC_` codes as human-friendly keycap legends in Top View.

- **2026-04-28**:
  - Added explicit MCU profile support for the "NRF52840 Dev Board (nice!nano V2.0 Compatible)" to `KeyVisualizer_v2.html` mirroring `nice_nano` pins, resolving a feature request.
  - Implemented a dynamic "Hardware Peripherals" panel in `KeyVisualizer_v2.html` to support OLED screens (I2C) and Rotary Encoders. The panel gracefully falls back to recommended MCU-specific pins if explicit properties (`currentJSON.i2c` / `currentJSON.encoder`) are missing from the configuration JSON.
  - Added an interactive "Assign Encoders" feature allowing users to visually click on matrix keys to convert them into Rotary Encoders. The tool automatically updates tooltips to reflect both the matrix click wiring and the rotary MCU wiring, significantly improving UX over static dropdowns.
  - Excluded encoder positions from the SVG Matrix wiring loop so that matrix wires correctly bypass the physical encoder locations. Corrected default `boardProfiles` to use strictly free MCU pins to prevent tooltip overlaps with default matrix pins.
  - Implemented Export features: `💾 Save JSON` dynamically injects the user's interactive encoder and I2C pin assignments into their configuration and downloads it. `📄 Copy C Code` generates the standard QMK `encoder_update_user` boilerplate for the `keymap.c` file based on the number of assigned encoders.
- **2026-05-09**:
  - Implemented the Automatic Keycode Mapping & Translation Plan proposed in the previous session.
  - Added a `⬇️ Fetch` button to automatically download the default `keymap.c` from the QMK GitHub repository (`https://raw.githubusercontent.com/qmk/qmk_firmware/master/keyboards/${kbName}/keymaps/default/keymap.c`) after a layout JSON is loaded.
  - Built a robust `translateToZmk` dictionary mapping common `KC_` codes, `MO()`, `LT()`, and `MT()` directly to ZMK devicetree syntax (`&kp`, `&mo`, `&lt`, `&mt`).
  - Rewrote the Export logic for ZMK (`.keymap`) and QMK (`keymap.c`) to iterate over the `parsedKeymaps` state instead of defaulting all matrices to `&trans` or `KC_TRNS`. Now exports correctly generated multi-layer layout files mimicking the user's custom keycode mappings.
  - Implemented Matrix Hardware Intelligence: added `validateMatrixIntegrity()` which checks `activeRows` and `activeCols` lengths against the MCU `boardProfiles` physical pin limit. Shows a red warning banner and marks "N/A" on keys if the matrix requires more pins than the board has.
  - Implemented Matrix Integrity Intelligence: added logic to parse the full layout and visually highlight keys (red border, tooltip alert) that share the exact same `[row, col]` coordinate, preventing handwiring ghosting errors.
  - Implemented `Import KLE` Feature & Auto-Router: users can paste a raw KLE array. The parser reads it, builds the physical layout, and executes a split/monolithic algorithm that automatically groups keys by `y`/`x` coordinates to generate QMK `[row, col]` assignments. It also calculates required matrix sizes and automatically assigns the best-fitting MCU from `boardProfiles`.
  - Refined KLE Parser: Updated the parser to properly handle KLE `rx` and `ry` coordinate resets, which fixed the issue of thumb cluster keys overlapping when importing complex rotated layouts. The `r` rotation parameter is safely ignored to keep the generated logical map flat and readable.
  - UI Cleanup: Removed the encoder and OLED peripheral logic from the visualizer to focus purely on matrix routing. Added a target board selection dropdown to the KLE import modal. Added `waveshare_2040_plus` to the supported boards.
  - Bugfix: Fixed Auto-Router duplicate matrix coordinate issue caused by relying on `Math.floor(key.y)` for the row index, which incorrectly mapped keys with slightly offset Y coordinates to the same physical matrix row. Replaced with an incremental row counter.

- **2026-05-17**:
  - Code Optimization: Refactored `KeyVisualizer_v2.js` by extracting repeated layout JSON parsing logic into a global `extractLayout` helper function to keep the codebase DRY.
  - Bugfix: Fixed a JavaScript `ReferenceError` when hovering over encoder keys in the visualizer. Brought the encoder pin evaluation variables (`enc_a`, `enc_b`, etc.) into `renderKeyboard` scope so interactive tooltips correctly display assigned MCU peripheral pins.
  - Bugfix: Corrected the `matrixRowsCount` fallback heuristic which was inadvertently breaking monolithic boards like Alice layouts. Unconditionally halving `max_r + 1` caused layout sides to flip; now relies on layout properties (`split`, `matrix_size.rows`, or `keyboard_name` matching "lily"/"split") to securely determine monolithic vs split matrix topologies.
  - Enhancement: Made KLE Auto-router split-line detection robust for Alice-style boards. Replaced simplistic `max_x / 2` midpoint with an algorithmic search for the largest horizontal coordinate gap in the middle 30% of the board, ensuring slightly overlapping rotated key clusters don't incorrectly partition into the wrong logical half.
  - Enhancement: Updated KLE parser to convert rotational coordinates (`r`, `rx`, `ry`) into absolute flat `X/Y` bounding box coordinates. This prevents Alice-layout clusters from overlapping physically in the visualizer when the rotation angle is ignored for rendering, effectively satisfying the "forget rotation, focus on XY" logic.
  - Bugfix: Fixed KLE Auto-Router wire crossovers on curved/Alice boards. The auto-router now ALWAYS physically splits the layout into two distinct halves before executing the spatial row-grouping algorithm. This prevents angled inner keys on the left half from mistakenly jumping across the physical gap and grouping with the right half. The halves are cleanly merged back together depending on the `isSplit` monolithic toggle.
  - Bugfix: Resolved the auto-router "snaking" wire bug where rotated keys in the same physical row failed to group correctly due to their vertical rotation stagger. The auto-router now uses `logical_x` and `logical_y` (the pure unrotated grid coordinates natively specified in the KLE JSON) strictly for its grouping, ensuring horizontal rows are perfectly identified, while the rotated `X/Y` coordinates are reserved exclusively for drawing the actual interface and wire connections.
  - Refactoring: Completely replaced the flawed snaking `routeGroup` logic with a new `spatialGridRoute` algorithm. This robust 2D spatial grid router drops logic-based tracking and strictly maps keys into orthogonal Matrix Rows and Columns purely based on their physical X and Y proximity buckets (using a `< 0.65u` tolerance). This perfectly matches real-world handwiring layouts (like Vial) by creating straight columns that drop down the keyboard and rows that span cleanly across it, fully supporting both split and monolithic wiring without wire crossovers.
  - Bugfix: Replaced the `spatialGridRoute` algorithm with a `traceRows` left-to-right raycasting algorithm. The spatial grid X-bucketing caused the "12 duplicate key coordinates" error on staggered columns because overlapping X coordinates merged adjacent columns into identical matrix locations. The new `traceRows` physically traces rows left-to-right using `X < 5.0` distance jumps and Y-similarity, guaranteeing perfect horizontal rows.
  - Enhancement: Implemented a smart Column Alignment algorithm (inspired by `kle-ng`'s collision shifting logic) to dynamically match keys across rows to the closest vertical column bucket (`< 1.0u` distance) while strictly enforcing a `usedCols` set per row. This flawlessly draws straight vertical column wires in the visualizer without cross-overs, while maintaining a mathematically perfect matrix layout with absolutely zero duplicate coordinate errors.
  - Feature: Added full support for Manual Matrix Overrides via KLE labels (identical to `kle-ng`). Users can now define exact custom matrices (like complex 6x18 optimized layouts) by simply typing `Row,Col` into the top-left label (Label 0) of the KLE. If all keys are manually labeled, the Auto-Router is completely bypassed, giving users 100% granular control over the generated visual wiring paths.
  - Enhancement: Implemented Contiguous Dynamic Pin Allocation for microcontroller board profiles. Previously, boards like the Waveshare 2040 Plus strictly segregated their 26 GPIO pins into 'rows' and 'cols', causing Pin Limit errors on highly asymmetric custom matrices (like 6x18). The logic now dynamically combines all available GPIO pins into a contiguous array and allocates them sequentially (e.g., Rows: GP0-GP5, Cols: GP6-GP22), fully supporting dense matrices and complying with QMK hardware design rules for contiguous debugging.
  - Feature: Developed a Matrix Compression algorithm for the spatial Auto-Router. Previously, curved ergonomic layouts (like Alice) shattered into dozens of physical rows and columns because the raycaster couldn't connect angled keys. The new algorithm compresses the sparse physical matrix into the tightest possible logical grid by merging non-overlapping rows and columns. This successfully shrinks a 12x16 raw Alice matrix down to an ultra-dense 8x14 logical matrix, effortlessly passing the 26-pin limit of the Waveshare 2040 Plus while strictly preserving Rule 9 (zero coordinate duplication).
  - REPLACED traceRows raycasting with gridRoute Y-band+X-band bucketing (ROW_TOLERANCE=0.7u, COL_TOLERANCE=0.55u). Now correctly produces 5x18 matrix (23 pins) matching reference handwiring schematic. Zero duplicate coords. Monolithic-only routing (no split logic needed).
  - Fixed waveshare_2040_plus profile: added GP29 (27 total usable GPIO). Fixed auto-selection logic to use total-pin-capacity check (totalPins >= requiredPins) as fallback when strict rows/cols segregation fails asymmetric matrices. Dynamic contiguous allocation: Rows GP0-GP4, Cols GP5-GP22 for 5x18.
  - [2026-05-17] SPLIT KEYBOARD FIX: Restored isSplit routing branch. Left half: gridRoute(leftHalves, 0, 0). Right half: gridRoute(rightHalves, perHalfRows, 0) — rows restart, cols shared (0-N). perHalfRows stored separately. Pin allocation uses perHalfRows (not numLogicalRows) for capacity checks — fixes "Pin Limit Exceeded" on nice!nano (6 physical rows per MCU is enough for 4-row-per-half Lily58). JSON stores split:true.
  - [2026-05-17] EXPORT CLEANUP: Removed all legacy export buttons (Save JSON, Export keymap.c, Export ZMK .overlay, Export vial.json, Porting Guide) and their 4 modal dialogs, all JS handlers, mapPinToZMK helper. JS shrunk 1550→1035 lines.
  - [2026-05-17] VIAL ZIP EXPORT: Added "Export Vial ZIP" button (shown after KLE import). Generates 9-file ZIP per official Vial docs: {kbName}/config.h, info.json, keyboard.h, rules.mk + keymaps/default/keymap.c + keymaps/vial/{keymap.c, rules.mk, config.h, vial.json}. Key rules: VIA_ENABLE+VIAL_ENABLE only in keymaps/vial/rules.mk, VIAL_KEYBOARD_UID in keymaps/vial/config.h, vial.json uses KLE format with "row,col" labels. Uses JSZip CDN for in-browser ZIP creation. MCU auto-detected from boardSelect.
